{
  "name": "teammate-voices-monorepo",
  "version": "1.0.0",
  "description": "Teammate Voices Monorepo - Design System + Applications",
  "private": true,
  "workspaces": [
    "packages/teammate-voices",
    "packages/workforce-management"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "dev:voices": "npm -w teammate-voices run dev",
    "dev:workforce": "npm -w workforce-management run dev",
    "build": "npm run build --workspaces",
    "build:voices": "npm -w teammate-voices run build",
    "lint": "npm run lint --workspaces",
    "db:up": "docker compose -f docker-compose.db.yml up -d",
    "db:down": "docker compose -f docker-compose.db.yml down",
    "docker:up": "docker compose -f docker-compose.yml up -d",
    "docker:down": "docker compose -f docker-compose.yml down",
    "docker:prod": "docker compose -f docker-compose.prod.yml up -d",
    "docker:prod:down": "docker compose -f docker-compose.prod.yml down"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
